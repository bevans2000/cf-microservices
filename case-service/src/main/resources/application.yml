spring:
  jpa:
    generate-ddl: true
  data:
    rest:
      basePath: /api
eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
server:
  port: 8081

---
spring:
  profiles: cloud
server:
  port: 8080  
eureka:
  instance:
    hostname: ${APPLICATION_DOMAIN}
    nonSecurePort: 80
  client:
    serviceUrl:
      defaultZone: https://eureka-server-ons.trial.cf.paas.alphagov.co.uk/eureka